<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<hr>
<%= link_to "Módosítás", edit_post_path(@post), :class => 'btn btn-primary' %>
<%= link_to "Törlés", post_path(@post),method: :delete, data: {confirm: 'Biztos vagy benne?'}, :class => 'btn btn-danger' %>
<hr>
<%= render 'comments/comments' %>
<%= render 'comments/form' %>